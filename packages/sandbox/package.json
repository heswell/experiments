{
  "name": "@heswell/sandbox",
  "version": "1.1.3",
  "private": true,
  "description": "Heswell Sandbox",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist/*",
    "serve": "serve",
    "build": "esbuild ./src/index.jsx --bundle --sourcemap --target=es2020 --avoid-tdz --keep-names --define:process.env.NODE_ENV='\"development\"' --outfile=./dist/main.js"
  },
  "devDependencies": {
    "esbuild": "^0.8.7",
    "serve": "^11.3.2"
  },
  "dependencies": {
    "styled-components": "^5.2.1"
  }
}
